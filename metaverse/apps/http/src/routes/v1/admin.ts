import { Router } from "express";

export const adminRouter = Router();

adminRouter.post("/element", (request, response) => {});

adminRouter.put("/element/:elementId", (request, response) => {});

adminRouter.get("/avatar", (request, response) => {});

adminRouter.get("/map", (request, response) => {});
